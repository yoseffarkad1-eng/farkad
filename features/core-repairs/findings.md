# The fifteen findings — reviewed at `c744d49`, each reproduced with tests/harness.mjs

Four reviewers by area, every finding verified by an independent agent that tried to refute
it; none was refuted. Line numbers are at `c744d49`.

| # | sev | where | problem | fix |
|---|---|---|---|---|
| 1 | P1 | js/sync/sync.js:1402 | `_baseDoc` is memory-only and set only by `noteRevision`, so an edit queued before the session's first snapshot (offline at open) records `base: null`; the pre-send hold exempts null (2903); when the phone later hears another device's newer value and flushes, its stale value is accepted at the current revision and both phones say synced. Reproduced: cloud ends `p_00,p_02`, winner was `p_00,p_01`. | Do not record null when no snapshot was heard: take the base from the durable local value at the path, or record a distinct "unheard" marker and HOLD such a path whenever the server now holds another device's value. Test: reopen → edit → hear → flush in tests/contested.test.mjs. |
| 2 | P2 | js/sync/sync.js:2892 | "someone else wrote it" is decided on the whole document's `updatedBy`; an unrelated own write makes this device the last writer, and after a reopen (hold marker refused earlier) the contested path is sent over the other phone's correction. | Decide per path (server value ≠ recorded base and not one of this device's own acknowledged values), or drop the document-level gate. Test: refused hold marker, own unrelated write, reopen, hear, flush → held. |
| 3 | P2 | js/sync/sync.js:2920 | A pre-send hold (movedUnder) sets no status: the phone reads «מחובר. יש רישומים שעדיין נשלחים.» indefinitely with nothing sending and no retry. | Route the hold through `fail()` with `error.contested`, or re-derive status in the holding branch. Pin: status 'contested' and its line on an idle phone. |
| 4 | P2 | js/sync/sync.js:2903 | A queued day with `base === null` is never held, so two phones recording the same worker on the same day offline: the second to return silently replaces the first (came-back-later timing); the in-flight timing is held. | Treat null base as "server held nothing": hold when the server now holds another device's value. Test: came-back-later variant of tests/cas.test.mjs:321. |
| 5 | P2 | js/sync/sync.js:3242 | A replayed write (answered from its receipt, no snapshot) acknowledges and prunes but never re-adopts the latest snapshot; if another phone corrected the path meanwhile, this phone shows its old value and says synced. | After a replay ack, re-run receive on `_latestRaw` (or adopt the answer's document). Test: lost update, other phone replaces, retry → local equals cloud. |
| 6 | P2 | js/state.js:827 | The guard `schedule.ledger.unreadable[id] === undefined` reads `Object.prototype` for `__proto__`, so `keepUnder` never runs: evidence held under `__proto__` is dropped at reopen and overwritten by the first save after acknowledgement. | Own-key guard (`hasOwnProperty`); test the reopened schedule owns `__proto__` and an ack+commit keeps the evidence on disk; allow-list `scheduleData:v2:poison:*` in the rescue sweep. |
| 7 | P2 | js/state.js:754 | Re-derivation quarantines a NEW copy on every reopen; `quarantineRecord` never overwrites, so after 20 opens the loop exhausts, `mustHold` sticks, `acknowledge()` returns false and the device is unwritable with a false "no room" message. | Register the existing copy instead of quarantining again (or skip when a verified identical copy exists). Test: 21 reopens still release. |
| 8 | P3 | js/model/schema.js:388 | `poisonedContainers` never looks at the `days` map itself; `normaliseSchedule` drops any non-date key silently — a day lost with no report. | `look(days, 'days')`; hold and report non-date keys instead of dropping. |
| 9 | P3 | js/state.js:959 | `normaliseLayer` copies with a bare assignment, so an adopted poisoned layer is reparented; deduped `Recovery.damaged` means the same document arriving again is adopted with the row missing and no second report. | `keepUnder`/skip poison segments in the copies; re-report re-arrival. |
| 10 | P3 | js/sync/sync.js:4881 | The post-persist held-aside check returns through `fail()` before `archiveDaily`, the identity repairs and the post-snapshot flush — on every snapshot, including after acknowledgement. | Report without returning; return only when `Recovery.blocked()` is actually true. |
| 11 | P3 | js/ui/share.js:495 | A restore file with a poisoned map passes `fullScheduleProblems`, then `normaliseSchedule` puts the DEVICE into recovery hold before `replaceEverything` refuses with the wrong sentence ("no room"). | Check `poisonedContainers` at the door and refuse the file with its own sentence. |
| 12 | P3 | js/sync/sync.js:3266 | After a replayed send the phone keeps status 'error' with an empty queue (success path withholds 'synced' when already 'error'; a replay echoes no snapshot). | Set 'synced' when the queue empties on a successful send, or reset to 'connecting' at retry start. Same-session test in tests/receipt.test.mjs. |
| 13 | P3 | js/sync/sync.js:3560 | The update after a lost create race is not routed through `onFailure`; its conflict escapes and shows "sync error (N pending)" for a ladder interval. | Pass `onFailure` into `createDocument`'s already-exists branch (or note the winner's revision first). |
| 14 | P3 | tests/README.md:3 | Stale counts (35/49/3 emulator) that CLAUDE.md's d202cfb principle removed elsewhere; at c744d49 it is 36/50/5. | Commit-scope or remove the counts; correct the words. |
| 15 | P3 | docs/releases.md:13 | Only v79 is described; v80–v86 are served and absent; v91 caveats are presented as current. | Entries for v80–v86; v91/v93/v94 where they land. |

Full text of every finding, fix and reproduction: the review run's result
(`tasks/wapvk1pe2.output` in the session that produced this file).
