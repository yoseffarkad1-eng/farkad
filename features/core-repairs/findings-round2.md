# Round two — the merged v94 tree reviewed at `9f11abf`, seven findings, none refuted

Three reviewers (the sync interplay, the poison doors, the shell and the compact day), every
finding verified by an independent agent that tried to refute it. Line numbers are at `9f11abf`.

| # | sev | where | problem | fix |
|---|---|---|---|---|
| R1 | P1 | js/sync/sync.js:3761 | The loser of a create race replaces the winner's day silently: `createDocument`'s already-exists branch (d669a5a) reads the winner's document, notes its revision and sends the queued patch as an ordinary update — `movedUnder` is never asked of the document it just read, and no conflict reaches `contestedPaths` because the learned revision makes the update valid. Two phones on a new project, same worker, same day: the winner's day is gone from every phone, both synced. Pre-existing on the base (finding 4's fix does not reach this timing). | After `noteRevision(fresh)`, ask the pre-send question of `fresh` for every `days.`/`ledger.` path in the patch (`movedUnder` → `holdContested` + `fail` with `error.contested`, drop from the patch and from `sent`), or throw a conflict carrying `document: fresh` into `onFailure`. Test beside 'same moment' / 'coming back later' in tests/cas.test.mjs: two phones, empty project, same day. |
| R2 | P2 | js/sync/sync.js:3402 | A phone whose snapshot listener has died reports «מסונכרן» after its next successful send: e2d9d39 made `setStatus('synced')` unconditional, `honestStatusFor` knows nothing about the listener, and `connect()` routes the adapter's onError into `fail()` with no resubscription. The base kept 'error'. | A listener-dead flag set by the subscribe onError (cleared only by a successful resubscribe) that `honestStatusFor` refuses 'synced' under, or resubscribe on the retry ladder. Pin in tests/status.test.mjs: listener error → own write lands → other phone corrects → not 'synced'. |
| R3 | P2 | js/model/schema.js:385 | A poison sighting's identity is the bytes of the WHOLE map, so once a poisoned layer is in the cloud document every sibling edit by any phone is a "new sighting": a new quarantine copy, `acknowledged=false`, writes blocked, the snapshot refused — the phone never adopts the other phones' work on that day. | Identity = the bytes under the poisoned name (`json: JSON.stringify(map[name])`, `at` per name); a sibling edit is the same sighting answered from the first copy. Pin: acknowledge, then 21 differing sibling snapshots → adopted, not re-blocked. |
| R4 | P2 | js/ui/share.js:1533 | The rescue door holds the READING phone for a file it only previewed: `importBackup` reads the file before `askConfirm`, and `scheduleFromRecoveryRecords` runs `normaliseSchedule`, which reports to `Recovery.evidence` (copy written, writes blocked); on cancel the phone stays held, and re-held at every reopen. | A collect-only mode for the rescue rebuild (findings returned on the result, not reported); hand them to `Recovery.evidence` only after the person confirms and the local replace stage has landed. On cancel nothing touches the disk. |
| R5 | P3 | js/sync/sync.js:2630 | After a poisoned snapshot is acknowledged mid-session nothing re-adopts it: the refused persist failed with 'no room', `releaseRecoveryHold` only resets status, no listener re-fires, `_latestRaw` is never re-run; the readable days it carried stay off the screen until the cloud changes again. | In `releaseRecoveryHold`/`acknowledge`, when the last snapshot was refused for a hold, re-run `receive(this._latestRaw)` the way `readoptAfter` does. |
| R6 | P2 | js/app.js:139 | `renderSyncChip` tests the queue-count suffix before the sentence, so the contested line — which the pre-send hold now raises on an idle phone, always with ≥1 held operation counted by `pendingCount()` — renders the chip as «ממתין לשליחה», never «דורש הכרעה». | Recognise the sentence before the count («הנתונים השתנו במכשיר אחר» → «דורש הכרעה»; «הסנכרון מושהה» → its own label or deliberately none); pin in tests/smoke.mjs with the exact contested line. |
| R7 | P3 | docs/releases.md:15 | No v94 entry although the tree is stamped v94 and changes five cached files under the compact day; the preamble's "two builds are candidates" and the v93 entry's "tip of cd-work" are stale. | A `## v94 — CANDIDATE, not served` entry with the gate's measured counts at the SHA it ran on, and the two wordings corrected. |

Full text of every finding and its reproduction: the review run's result (`tasks/w2qwc1qtc.output` in
the session that produced this file).
